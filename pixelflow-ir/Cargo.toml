[package]
name = "pixelflow-ir"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"], optional = true }

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[features]
default = ["std", "alloc"]
std = []
alloc = []

