[package]
name = "actor-scheduler-macros"
version = "0.1.0"
edition = "2024"

[lints]
workspace = true

[lib]
proc-macro = true
bench = false

[dependencies]
# No external dependencies - only proc_macro from std
